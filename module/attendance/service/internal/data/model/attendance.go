// Code generated by ent, DO NOT EDIT.

package model

import (
	"fmt"
	"mall-go/module/attendance/service/internal/data/model/attendance"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Attendance is the model entity for the Attendance schema.
type Attendance struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Karyacode holds the value of the "karyacode" field.
	Karyacode string `json:"karyacode,omitempty"`
	// ClockInTime holds the value of the "clock_in_time" field.
	ClockInTime time.Time `json:"clock_in_time,omitempty"`
	// ClockOutTime holds the value of the "clock_out_time" field.
	ClockOutTime *time.Time `json:"clock_out_time,omitempty"`
	// Status holds the value of the "status" field.
	Status *string `json:"status,omitempty"`
	// EffectiveWorkDurationMinutes holds the value of the "effective_work_duration_minutes" field.
	EffectiveWorkDurationMinutes *int `json:"effective_work_duration_minutes,omitempty"`
	// OvertimeMinutes holds the value of the "overtime_minutes" field.
	OvertimeMinutes int `json:"overtime_minutes,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes *string `json:"notes,omitempty"`
	// Location holds the value of the "location" field.
	Location *string `json:"location,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Attendance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case attendance.FieldID, attendance.FieldEffectiveWorkDurationMinutes, attendance.FieldOvertimeMinutes:
			values[i] = new(sql.NullInt64)
		case attendance.FieldKaryacode, attendance.FieldStatus, attendance.FieldNotes, attendance.FieldLocation:
			values[i] = new(sql.NullString)
		case attendance.FieldClockInTime, attendance.FieldClockOutTime, attendance.FieldCreatedAt, attendance.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Attendance fields.
func (a *Attendance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case attendance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int64(value.Int64)
		case attendance.FieldKaryacode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field karyacode", values[i])
			} else if value.Valid {
				a.Karyacode = value.String
			}
		case attendance.FieldClockInTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field clock_in_time", values[i])
			} else if value.Valid {
				a.ClockInTime = value.Time
			}
		case attendance.FieldClockOutTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field clock_out_time", values[i])
			} else if value.Valid {
				a.ClockOutTime = new(time.Time)
				*a.ClockOutTime = value.Time
			}
		case attendance.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				a.Status = new(string)
				*a.Status = value.String
			}
		case attendance.FieldEffectiveWorkDurationMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field effective_work_duration_minutes", values[i])
			} else if value.Valid {
				a.EffectiveWorkDurationMinutes = new(int)
				*a.EffectiveWorkDurationMinutes = int(value.Int64)
			}
		case attendance.FieldOvertimeMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field overtime_minutes", values[i])
			} else if value.Valid {
				a.OvertimeMinutes = int(value.Int64)
			}
		case attendance.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				a.Notes = new(string)
				*a.Notes = value.String
			}
		case attendance.FieldLocation:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field location", values[i])
			} else if value.Valid {
				a.Location = new(string)
				*a.Location = value.String
			}
		case attendance.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				a.CreatedAt = value.Time
			}
		case attendance.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				a.UpdatedAt = value.Time
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Attendance.
// This includes values selected through modifiers, order, etc.
func (a *Attendance) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// Update returns a builder for updating this Attendance.
// Note that you need to call Attendance.Unwrap() before calling this method if this Attendance
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Attendance) Update() *AttendanceUpdateOne {
	return NewAttendanceClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Attendance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Attendance) Unwrap() *Attendance {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("model: Attendance is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Attendance) String() string {
	var builder strings.Builder
	builder.WriteString("Attendance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("karyacode=")
	builder.WriteString(a.Karyacode)
	builder.WriteString(", ")
	builder.WriteString("clock_in_time=")
	builder.WriteString(a.ClockInTime.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := a.ClockOutTime; v != nil {
		builder.WriteString("clock_out_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := a.Status; v != nil {
		builder.WriteString("status=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := a.EffectiveWorkDurationMinutes; v != nil {
		builder.WriteString("effective_work_duration_minutes=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("overtime_minutes=")
	builder.WriteString(fmt.Sprintf("%v", a.OvertimeMinutes))
	builder.WriteString(", ")
	if v := a.Notes; v != nil {
		builder.WriteString("notes=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := a.Location; v != nil {
		builder.WriteString("location=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(a.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(a.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Attendances is a parsable slice of Attendance.
type Attendances []*Attendance
