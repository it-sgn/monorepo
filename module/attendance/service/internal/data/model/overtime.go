// Code generated by ent, DO NOT EDIT.

package model

import (
	"fmt"
	"mall-go/module/attendance/service/internal/data/model/overtime"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Overtime is the model entity for the Overtime schema.
type Overtime struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Karyacode holds the value of the "karyacode" field.
	Karyacode string `json:"karyacode,omitempty"`
	// OvertimeStartTime holds the value of the "overtime_start_time" field.
	OvertimeStartTime time.Time `json:"overtime_start_time,omitempty"`
	// OvertimeEndTime holds the value of the "overtime_end_time" field.
	OvertimeEndTime time.Time `json:"overtime_end_time,omitempty"`
	// DurationMinutes holds the value of the "duration_minutes" field.
	DurationMinutes int `json:"duration_minutes,omitempty"`
	// ApprovedBy holds the value of the "approved_by" field.
	ApprovedBy string `json:"approved_by,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes *string `json:"notes,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Overtime) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case overtime.FieldID, overtime.FieldDurationMinutes:
			values[i] = new(sql.NullInt64)
		case overtime.FieldKaryacode, overtime.FieldApprovedBy, overtime.FieldStatus, overtime.FieldNotes:
			values[i] = new(sql.NullString)
		case overtime.FieldOvertimeStartTime, overtime.FieldOvertimeEndTime, overtime.FieldCreatedAt, overtime.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Overtime fields.
func (o *Overtime) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case overtime.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			o.ID = int64(value.Int64)
		case overtime.FieldKaryacode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field karyacode", values[i])
			} else if value.Valid {
				o.Karyacode = value.String
			}
		case overtime.FieldOvertimeStartTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field overtime_start_time", values[i])
			} else if value.Valid {
				o.OvertimeStartTime = value.Time
			}
		case overtime.FieldOvertimeEndTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field overtime_end_time", values[i])
			} else if value.Valid {
				o.OvertimeEndTime = value.Time
			}
		case overtime.FieldDurationMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field duration_minutes", values[i])
			} else if value.Valid {
				o.DurationMinutes = int(value.Int64)
			}
		case overtime.FieldApprovedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field approved_by", values[i])
			} else if value.Valid {
				o.ApprovedBy = value.String
			}
		case overtime.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				o.Status = value.String
			}
		case overtime.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				o.Notes = new(string)
				*o.Notes = value.String
			}
		case overtime.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				o.CreatedAt = value.Time
			}
		case overtime.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				o.UpdatedAt = value.Time
			}
		default:
			o.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Overtime.
// This includes values selected through modifiers, order, etc.
func (o *Overtime) Value(name string) (ent.Value, error) {
	return o.selectValues.Get(name)
}

// Update returns a builder for updating this Overtime.
// Note that you need to call Overtime.Unwrap() before calling this method if this Overtime
// was returned from a transaction, and the transaction was committed or rolled back.
func (o *Overtime) Update() *OvertimeUpdateOne {
	return NewOvertimeClient(o.config).UpdateOne(o)
}

// Unwrap unwraps the Overtime entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (o *Overtime) Unwrap() *Overtime {
	_tx, ok := o.config.driver.(*txDriver)
	if !ok {
		panic("model: Overtime is not a transactional entity")
	}
	o.config.driver = _tx.drv
	return o
}

// String implements the fmt.Stringer.
func (o *Overtime) String() string {
	var builder strings.Builder
	builder.WriteString("Overtime(")
	builder.WriteString(fmt.Sprintf("id=%v, ", o.ID))
	builder.WriteString("karyacode=")
	builder.WriteString(o.Karyacode)
	builder.WriteString(", ")
	builder.WriteString("overtime_start_time=")
	builder.WriteString(o.OvertimeStartTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("overtime_end_time=")
	builder.WriteString(o.OvertimeEndTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("duration_minutes=")
	builder.WriteString(fmt.Sprintf("%v", o.DurationMinutes))
	builder.WriteString(", ")
	builder.WriteString("approved_by=")
	builder.WriteString(o.ApprovedBy)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(o.Status)
	builder.WriteString(", ")
	if v := o.Notes; v != nil {
		builder.WriteString("notes=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(o.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(o.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Overtimes is a parsable slice of Overtime.
type Overtimes []*Overtime
