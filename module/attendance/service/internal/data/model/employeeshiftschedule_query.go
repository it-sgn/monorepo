// Code generated by ent, DO NOT EDIT.

package model

import (
	"context"
	"fmt"
	"mall-go/module/attendance/service/internal/data/model/employeeshiftschedule"
	"mall-go/module/attendance/service/internal/data/model/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EmployeeShiftScheduleQuery is the builder for querying EmployeeShiftSchedule entities.
type EmployeeShiftScheduleQuery struct {
	config
	ctx        *QueryContext
	order      []employeeshiftschedule.OrderOption
	inters     []Interceptor
	predicates []predicate.EmployeeShiftSchedule
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmployeeShiftScheduleQuery builder.
func (essq *EmployeeShiftScheduleQuery) Where(ps ...predicate.EmployeeShiftSchedule) *EmployeeShiftScheduleQuery {
	essq.predicates = append(essq.predicates, ps...)
	return essq
}

// Limit the number of records to be returned by this query.
func (essq *EmployeeShiftScheduleQuery) Limit(limit int) *EmployeeShiftScheduleQuery {
	essq.ctx.Limit = &limit
	return essq
}

// Offset to start from.
func (essq *EmployeeShiftScheduleQuery) Offset(offset int) *EmployeeShiftScheduleQuery {
	essq.ctx.Offset = &offset
	return essq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (essq *EmployeeShiftScheduleQuery) Unique(unique bool) *EmployeeShiftScheduleQuery {
	essq.ctx.Unique = &unique
	return essq
}

// Order specifies how the records should be ordered.
func (essq *EmployeeShiftScheduleQuery) Order(o ...employeeshiftschedule.OrderOption) *EmployeeShiftScheduleQuery {
	essq.order = append(essq.order, o...)
	return essq
}

// First returns the first EmployeeShiftSchedule entity from the query.
// Returns a *NotFoundError when no EmployeeShiftSchedule was found.
func (essq *EmployeeShiftScheduleQuery) First(ctx context.Context) (*EmployeeShiftSchedule, error) {
	nodes, err := essq.Limit(1).All(setContextOp(ctx, essq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{employeeshiftschedule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) FirstX(ctx context.Context) *EmployeeShiftSchedule {
	node, err := essq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmployeeShiftSchedule ID from the query.
// Returns a *NotFoundError when no EmployeeShiftSchedule ID was found.
func (essq *EmployeeShiftScheduleQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = essq.Limit(1).IDs(setContextOp(ctx, essq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{employeeshiftschedule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) FirstIDX(ctx context.Context) int64 {
	id, err := essq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmployeeShiftSchedule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmployeeShiftSchedule entity is found.
// Returns a *NotFoundError when no EmployeeShiftSchedule entities are found.
func (essq *EmployeeShiftScheduleQuery) Only(ctx context.Context) (*EmployeeShiftSchedule, error) {
	nodes, err := essq.Limit(2).All(setContextOp(ctx, essq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{employeeshiftschedule.Label}
	default:
		return nil, &NotSingularError{employeeshiftschedule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) OnlyX(ctx context.Context) *EmployeeShiftSchedule {
	node, err := essq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmployeeShiftSchedule ID in the query.
// Returns a *NotSingularError when more than one EmployeeShiftSchedule ID is found.
// Returns a *NotFoundError when no entities are found.
func (essq *EmployeeShiftScheduleQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = essq.Limit(2).IDs(setContextOp(ctx, essq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{employeeshiftschedule.Label}
	default:
		err = &NotSingularError{employeeshiftschedule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := essq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmployeeShiftSchedules.
func (essq *EmployeeShiftScheduleQuery) All(ctx context.Context) ([]*EmployeeShiftSchedule, error) {
	ctx = setContextOp(ctx, essq.ctx, ent.OpQueryAll)
	if err := essq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmployeeShiftSchedule, *EmployeeShiftScheduleQuery]()
	return withInterceptors[[]*EmployeeShiftSchedule](ctx, essq, qr, essq.inters)
}

// AllX is like All, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) AllX(ctx context.Context) []*EmployeeShiftSchedule {
	nodes, err := essq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmployeeShiftSchedule IDs.
func (essq *EmployeeShiftScheduleQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if essq.ctx.Unique == nil && essq.path != nil {
		essq.Unique(true)
	}
	ctx = setContextOp(ctx, essq.ctx, ent.OpQueryIDs)
	if err = essq.Select(employeeshiftschedule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) IDsX(ctx context.Context) []int64 {
	ids, err := essq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (essq *EmployeeShiftScheduleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, essq.ctx, ent.OpQueryCount)
	if err := essq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, essq, querierCount[*EmployeeShiftScheduleQuery](), essq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) CountX(ctx context.Context) int {
	count, err := essq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (essq *EmployeeShiftScheduleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, essq.ctx, ent.OpQueryExist)
	switch _, err := essq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("model: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (essq *EmployeeShiftScheduleQuery) ExistX(ctx context.Context) bool {
	exist, err := essq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmployeeShiftScheduleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (essq *EmployeeShiftScheduleQuery) Clone() *EmployeeShiftScheduleQuery {
	if essq == nil {
		return nil
	}
	return &EmployeeShiftScheduleQuery{
		config:     essq.config,
		ctx:        essq.ctx.Clone(),
		order:      append([]employeeshiftschedule.OrderOption{}, essq.order...),
		inters:     append([]Interceptor{}, essq.inters...),
		predicates: append([]predicate.EmployeeShiftSchedule{}, essq.predicates...),
		// clone intermediate query.
		sql:  essq.sql.Clone(),
		path: essq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Karyacode string `json:"karyacode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmployeeShiftSchedule.Query().
//		GroupBy(employeeshiftschedule.FieldKaryacode).
//		Aggregate(model.Count()).
//		Scan(ctx, &v)
func (essq *EmployeeShiftScheduleQuery) GroupBy(field string, fields ...string) *EmployeeShiftScheduleGroupBy {
	essq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmployeeShiftScheduleGroupBy{build: essq}
	grbuild.flds = &essq.ctx.Fields
	grbuild.label = employeeshiftschedule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Karyacode string `json:"karyacode,omitempty"`
//	}
//
//	client.EmployeeShiftSchedule.Query().
//		Select(employeeshiftschedule.FieldKaryacode).
//		Scan(ctx, &v)
func (essq *EmployeeShiftScheduleQuery) Select(fields ...string) *EmployeeShiftScheduleSelect {
	essq.ctx.Fields = append(essq.ctx.Fields, fields...)
	sbuild := &EmployeeShiftScheduleSelect{EmployeeShiftScheduleQuery: essq}
	sbuild.label = employeeshiftschedule.Label
	sbuild.flds, sbuild.scan = &essq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmployeeShiftScheduleSelect configured with the given aggregations.
func (essq *EmployeeShiftScheduleQuery) Aggregate(fns ...AggregateFunc) *EmployeeShiftScheduleSelect {
	return essq.Select().Aggregate(fns...)
}

func (essq *EmployeeShiftScheduleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range essq.inters {
		if inter == nil {
			return fmt.Errorf("model: uninitialized interceptor (forgotten import model/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, essq); err != nil {
				return err
			}
		}
	}
	for _, f := range essq.ctx.Fields {
		if !employeeshiftschedule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("model: invalid field %q for query", f)}
		}
	}
	if essq.path != nil {
		prev, err := essq.path(ctx)
		if err != nil {
			return err
		}
		essq.sql = prev
	}
	return nil
}

func (essq *EmployeeShiftScheduleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmployeeShiftSchedule, error) {
	var (
		nodes = []*EmployeeShiftSchedule{}
		_spec = essq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmployeeShiftSchedule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmployeeShiftSchedule{config: essq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, essq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (essq *EmployeeShiftScheduleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := essq.querySpec()
	_spec.Node.Columns = essq.ctx.Fields
	if len(essq.ctx.Fields) > 0 {
		_spec.Unique = essq.ctx.Unique != nil && *essq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, essq.driver, _spec)
}

func (essq *EmployeeShiftScheduleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(employeeshiftschedule.Table, employeeshiftschedule.Columns, sqlgraph.NewFieldSpec(employeeshiftschedule.FieldID, field.TypeInt64))
	_spec.From = essq.sql
	if unique := essq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if essq.path != nil {
		_spec.Unique = true
	}
	if fields := essq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, employeeshiftschedule.FieldID)
		for i := range fields {
			if fields[i] != employeeshiftschedule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := essq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := essq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := essq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := essq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (essq *EmployeeShiftScheduleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(essq.driver.Dialect())
	t1 := builder.Table(employeeshiftschedule.Table)
	columns := essq.ctx.Fields
	if len(columns) == 0 {
		columns = employeeshiftschedule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if essq.sql != nil {
		selector = essq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if essq.ctx.Unique != nil && *essq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range essq.predicates {
		p(selector)
	}
	for _, p := range essq.order {
		p(selector)
	}
	if offset := essq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := essq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmployeeShiftScheduleGroupBy is the group-by builder for EmployeeShiftSchedule entities.
type EmployeeShiftScheduleGroupBy struct {
	selector
	build *EmployeeShiftScheduleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (essgb *EmployeeShiftScheduleGroupBy) Aggregate(fns ...AggregateFunc) *EmployeeShiftScheduleGroupBy {
	essgb.fns = append(essgb.fns, fns...)
	return essgb
}

// Scan applies the selector query and scans the result into the given value.
func (essgb *EmployeeShiftScheduleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, essgb.build.ctx, ent.OpQueryGroupBy)
	if err := essgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmployeeShiftScheduleQuery, *EmployeeShiftScheduleGroupBy](ctx, essgb.build, essgb, essgb.build.inters, v)
}

func (essgb *EmployeeShiftScheduleGroupBy) sqlScan(ctx context.Context, root *EmployeeShiftScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(essgb.fns))
	for _, fn := range essgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*essgb.flds)+len(essgb.fns))
		for _, f := range *essgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*essgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := essgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmployeeShiftScheduleSelect is the builder for selecting fields of EmployeeShiftSchedule entities.
type EmployeeShiftScheduleSelect struct {
	*EmployeeShiftScheduleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (esss *EmployeeShiftScheduleSelect) Aggregate(fns ...AggregateFunc) *EmployeeShiftScheduleSelect {
	esss.fns = append(esss.fns, fns...)
	return esss
}

// Scan applies the selector query and scans the result into the given value.
func (esss *EmployeeShiftScheduleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, esss.ctx, ent.OpQuerySelect)
	if err := esss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmployeeShiftScheduleQuery, *EmployeeShiftScheduleSelect](ctx, esss.EmployeeShiftScheduleQuery, esss, esss.inters, v)
}

func (esss *EmployeeShiftScheduleSelect) sqlScan(ctx context.Context, root *EmployeeShiftScheduleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(esss.fns))
	for _, fn := range esss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*esss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := esss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
