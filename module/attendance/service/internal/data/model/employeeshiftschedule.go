// Code generated by ent, DO NOT EDIT.

package model

import (
	"fmt"
	"mall-go/module/attendance/service/internal/data/model/employeeshiftschedule"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// EmployeeShiftSchedule is the model entity for the EmployeeShiftSchedule schema.
type EmployeeShiftSchedule struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Karyacode holds the value of the "karyacode" field.
	Karyacode string `json:"karyacode,omitempty"`
	// AssignmentDate holds the value of the "assignment_date" field.
	AssignmentDate time.Time `json:"assignment_date,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmployeeShiftSchedule) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case employeeshiftschedule.FieldID:
			values[i] = new(sql.NullInt64)
		case employeeshiftschedule.FieldKaryacode:
			values[i] = new(sql.NullString)
		case employeeshiftschedule.FieldAssignmentDate, employeeshiftschedule.FieldCreatedAt, employeeshiftschedule.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmployeeShiftSchedule fields.
func (ess *EmployeeShiftSchedule) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case employeeshiftschedule.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ess.ID = int64(value.Int64)
		case employeeshiftschedule.FieldKaryacode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field karyacode", values[i])
			} else if value.Valid {
				ess.Karyacode = value.String
			}
		case employeeshiftschedule.FieldAssignmentDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field assignment_date", values[i])
			} else if value.Valid {
				ess.AssignmentDate = value.Time
			}
		case employeeshiftschedule.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ess.CreatedAt = value.Time
			}
		case employeeshiftschedule.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ess.UpdatedAt = value.Time
			}
		default:
			ess.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EmployeeShiftSchedule.
// This includes values selected through modifiers, order, etc.
func (ess *EmployeeShiftSchedule) Value(name string) (ent.Value, error) {
	return ess.selectValues.Get(name)
}

// Update returns a builder for updating this EmployeeShiftSchedule.
// Note that you need to call EmployeeShiftSchedule.Unwrap() before calling this method if this EmployeeShiftSchedule
// was returned from a transaction, and the transaction was committed or rolled back.
func (ess *EmployeeShiftSchedule) Update() *EmployeeShiftScheduleUpdateOne {
	return NewEmployeeShiftScheduleClient(ess.config).UpdateOne(ess)
}

// Unwrap unwraps the EmployeeShiftSchedule entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ess *EmployeeShiftSchedule) Unwrap() *EmployeeShiftSchedule {
	_tx, ok := ess.config.driver.(*txDriver)
	if !ok {
		panic("model: EmployeeShiftSchedule is not a transactional entity")
	}
	ess.config.driver = _tx.drv
	return ess
}

// String implements the fmt.Stringer.
func (ess *EmployeeShiftSchedule) String() string {
	var builder strings.Builder
	builder.WriteString("EmployeeShiftSchedule(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ess.ID))
	builder.WriteString("karyacode=")
	builder.WriteString(ess.Karyacode)
	builder.WriteString(", ")
	builder.WriteString("assignment_date=")
	builder.WriteString(ess.AssignmentDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ess.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ess.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EmployeeShiftSchedules is a parsable slice of EmployeeShiftSchedule.
type EmployeeShiftSchedules []*EmployeeShiftSchedule
