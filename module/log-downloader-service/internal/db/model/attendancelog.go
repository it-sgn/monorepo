// Code generated by ent, DO NOT EDIT.

package model

import (
	"fmt"
	"mall-go/module/log-downloader-service/internal/db/model/attendancelog"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AttendanceLog is the model entity for the AttendanceLog schema.
type AttendanceLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int64 `json:"user_id,omitempty"`
	// DeviceIP holds the value of the "device_ip" field.
	DeviceIP string `json:"device_ip,omitempty"`
	// AttLog holds the value of the "att_log" field.
	AttLog time.Time `json:"att_log,omitempty"`
	// Status holds the value of the "status" field.
	Status       int `json:"status,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AttendanceLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case attendancelog.FieldID, attendancelog.FieldUserID, attendancelog.FieldStatus:
			values[i] = new(sql.NullInt64)
		case attendancelog.FieldDeviceIP:
			values[i] = new(sql.NullString)
		case attendancelog.FieldAttLog:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AttendanceLog fields.
func (al *AttendanceLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case attendancelog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			al.ID = int64(value.Int64)
		case attendancelog.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				al.UserID = value.Int64
			}
		case attendancelog.FieldDeviceIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_ip", values[i])
			} else if value.Valid {
				al.DeviceIP = value.String
			}
		case attendancelog.FieldAttLog:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field att_log", values[i])
			} else if value.Valid {
				al.AttLog = value.Time
			}
		case attendancelog.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				al.Status = int(value.Int64)
			}
		default:
			al.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AttendanceLog.
// This includes values selected through modifiers, order, etc.
func (al *AttendanceLog) Value(name string) (ent.Value, error) {
	return al.selectValues.Get(name)
}

// Update returns a builder for updating this AttendanceLog.
// Note that you need to call AttendanceLog.Unwrap() before calling this method if this AttendanceLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (al *AttendanceLog) Update() *AttendanceLogUpdateOne {
	return NewAttendanceLogClient(al.config).UpdateOne(al)
}

// Unwrap unwraps the AttendanceLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (al *AttendanceLog) Unwrap() *AttendanceLog {
	_tx, ok := al.config.driver.(*txDriver)
	if !ok {
		panic("model: AttendanceLog is not a transactional entity")
	}
	al.config.driver = _tx.drv
	return al
}

// String implements the fmt.Stringer.
func (al *AttendanceLog) String() string {
	var builder strings.Builder
	builder.WriteString("AttendanceLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", al.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", al.UserID))
	builder.WriteString(", ")
	builder.WriteString("device_ip=")
	builder.WriteString(al.DeviceIP)
	builder.WriteString(", ")
	builder.WriteString("att_log=")
	builder.WriteString(al.AttLog.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", al.Status))
	builder.WriteByte(')')
	return builder.String()
}

// AttendanceLogs is a parsable slice of AttendanceLog.
type AttendanceLogs []*AttendanceLog
